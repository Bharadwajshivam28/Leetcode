class Solution {
    public int removeElement(int[] nums, int val) {
        int ans = 0;
        int size = nums.length;
            for(int i = 0; i<size; i++){
            if(nums[i] != val){
                nums[ans] = nums[i];
                ans++;
            }
        }
    return ans;
    }
}
