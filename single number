class Solution {
    public int singleNumber(int[] nums) {
        int ans = 0;
        int size = nums.length;

        for(int i = 0; i<size; i++){
            ans = ans ^ nums[i];
        }
        return (int)ans;
    }
}
